#include <stdio.h>
#include <readline/readline.h>
#include <readline/history.h>

int	main(void)
{
	char *string;
	
	while (1)
	{
		string = readline("Minishell >");
		if (!strcmp(string, "wc"))
		{
			foijtjefjooifasoij
		}
		if (cd -> )

	return (0);
}
